# Release test matrix

The CI suite is intentionally independent of a Grim Dawn installation. Before a
public tag, the maintainer additionally runs `npm.cmd run test:release-local` on
a supported game installation and records the manual live-transfer results here.

## Automated gates

| Scenario | Evidence | Current result |
| --- | --- | --- |
| TypeScript/Vue production build | `npm run build` | Passed 2026-08-30 |
| Fresh database migration/idempotent reopen | desktop smoke, in-memory database | Passed |
| Atomic replacement, backup, rollback | helper write self-test in a unique temporary directory | Passed |
| Stale source hash | helper write self-test | Rejected without changing source |
| Invalid replacement | helper write self-test | Rejected without changing source |
| Live CSV serialization and semantic receipt hash | helper live-queue self-test | Passed, 18 fields |
| Offline live-queue recovery state machine | helper live-queue self-test with disposable queue root | Passed: pending, deposited, rejected, multi-item, stale semantic hash, and path escape guard |
| Unsupported live build negative control | helper fingerprint self-test | Passed: unknown `Game.dll` hash and known crashing hook fingerprint remain outside the verified allowlist |
| Acquisition producer paths | helper acquisition self-test with synthetic item, loot, monster, formula, and vendor records | Passed: closest direct producers win, reagent formulae are rejected, blueprint vendors remain distinct from direct stock, and equal-depth variants survive |
| Bundled hook/injector fingerprints | helper and package audits | Passed |
| Native VC++ prerequisite packaging | PE import allowlist, Microsoft signature/version gate, NSIS build, installed-payload hash audit, and isolated installer lifecycle | **Passed 2026-09-01:** hook imports `MSVCP140.dll`, `VCRUNTIME140.dll`, and `VCRUNTIME140_1.dll`; injector imports `VCRUNTIME140.dll`; release payload embedded Microsoft-signed x64 runtime `14.51.36247.0` with SHA-256 `843068991daaa1f73ad9f6239bce4d0f6a07a51f18c37ea2a867e9beca71295c`; installer skip-path, synthetic first launch, uninstall, and retained profile passed. Runtime-absent Windows VM execution remains pending in #66. |
| Native adapter endpoint-protection diagnostics | generated missing-hook/injector helper fixtures, IPC domain mapping, release-policy manifest gate, and external-tester procedure | **Passed 2026-09-01:** exact missing native filenames are reported with repair/reinstall and Windows Security Protection history guidance; tests require the warning not to recommend globally disabling security software. All Cairn-produced binaries remain explicitly unsigned for the initial beta and release creation rejects a mixed signing state. Fresh Windows 10/11 SmartScreen/Defender execution remains pending in #67. |
| Duplicate vault selection | desktop smoke | Rejected |
| Rejected retrieval state rollback | desktop smoke | Passed |
| Generated personal/special delivery journal | desktop smoke | Committed and rejected outcomes retained; queue identity persisted before acknowledgement |
| Lost helper response after a queued write | desktop smoke | Retained as `needs_recovery`; later writes blocked |
| Cairn restart after queued retrieval | disposable on-disk archive plus simulated durable deposited receipt | Passed: journal survived close/reopen, exact copy committed once, duplicate submit rejected |
| Grim Dawn exit during multi-supply delivery | simulated offline deposited/rejected receipts | Passed: one delivered and one rejected receipt reconciled to one committed operation; rejected receipt copied before acknowledgement |
| Repeated multi-item live ingest | simulated durable incoming receipts | Passed: two exact copies committed on first sync and zero duplicated on repeat sync |
| Stale or mismatched live receipt | simulated `unknown` semantic receipt state | Passed: operation and copy remain pending for audit; no write is unblocked |
| Database integrity after uncertain outcome | `PRAGMA quick_check` in desktop smoke | Passed |
| Archive backup, rotation, and restart restore | disposable on-disk archive in desktop smoke | Verified snapshot hashes and manifests; selected prior state restored; emergency pre-restore copy retained; invalid pending request quarantined without replacing the current archive |
| Archive roll cache and pinned-best persistence | desktop smoke | Passed |
| All discovered stash serializer round trips | desktop smoke | 6 passed |
| Representative ingest plans | desktop smoke | 5 passed without writing game files |
| Representative ingest/retrieval round trips | desktop smoke | 5 passed in memory |
| Packaged helper with external .NET lookup disabled | packaged-helper smoke | Passed |
| Windows application icon pipeline | deterministic SVG-to-ICO check, portable and NSIS builds, and extracted-resource pixel comparison | **Passed 2026-09-01:** the repo-owned Cairn mark generates nine Windows icon frames (16–256px); the development `BrowserWindow` and AppUserModelID use it; portable EXE, installed EXE, setup, uninstaller, and shortcut configuration use the same ICO; extracted portable, installed, and setup resources match its 32px frame pixel-for-pixel. |
| Empty Cairn user profile | packaged screenshot diagnostic | Passed 2026-08-30 |
| Missing game and missing saves | isolated helper discovery roots | Passed; host environment did not leak into result |
| Non-default Steam library and GOG install | synthetic filesystem discovery | Passed |
| Incomplete expansion layout | content-pack layout diagnostic | Safely ignored; recognized after required database and tags were present |
| Softcore-only, Hardcore-only, and mixed saves | isolated malformed-stash diagnostics | Passed; mode remained correctly classified without parsing unsafe bytes |
| Item Assistant migration | packaged app, synthetic GDIA database and queue, copied catalog seed | 4 copies imported (2 SC / 2 HC), 1 queue receipt retained, 1 unsupported record skipped, source unchanged |
| Repeated Item Assistant migration | packaged repeat gate plus isolated content-hash backup test | No duplicate vault item or journal; unchanged source reuses one fully rehashed backup; corrupted backup is rejected and replaced |
| Item Assistant source backup retention | generated source revisions and injected low-space result | Passed 2026-09-01: three distinct verified recovery points retained, latest always preserved, legacy and interrupted-publication backups adopted by hash, unmanaged files preserved, insufficient space rejected before copy |
| First-run onboarding persistence | isolated capability-limited preference-effect gate plus wide and narrow fresh-profile screenshot fixtures | Passed 2026-09-01: discovery, Item Assistant/continue-without-import choice, exact requested screenshot step, explicit preservation copy, archive/live and SC/HC model, skip/resume, version reset, and Settings reopen behavior are covered without reading or writing real game data; focus containment/restoration is implemented and remains a manual keyboard check |
| Unified notification queue | deterministic scheduler and renderer type gate | Passed 2026-09-01: one visible notice, duplicate coalescing, timer replacement, severity preemption, persistent transfer failures, recovery reminder resurfacing, and one announcement per changed activation |
| Renderer recovery and safe mode | deterministic startup-state test, typed build, isolated synthetic workspace failure, and 1280px/760px screenshots | **Passed 2026-09-01:** three consecutive unhealthy starts offer safe mode; healthy and stale-window starts reset safely; workspace failures retain the application shell and emit a redacted correlation ID; safe mode hides experimental tools and pauses auto-connect; interface reset preserves planner, to-do, source, retrieval, onboarding, archive, save, stash, and backup state |
| Durable planner preferences | `npm run test:preferences`, `npm run test:preference-store`, full `npm run verify`, two synthetic browser origins, corrupt-primary fixture, failed-file-write mirror fixture, and 20 durable edits | **Passed 2026-09-02:** packaged and development origins merged once by stable planner/to-do IDs into one user-data document; a later deletion did not resurrect when the old origin returned; a strictly newer same-origin browser mirror recovered an unsaved canonical-file update; recoverable legacy profile objects received bounded defaults instead of being discarded; corrupt primary storage recovered from the previous valid envelope; planner/to-do changes retained exactly the newest 12 snapshots and left no temporary files. |
| Cached startup and search | packaged app, isolated cached 5,525-item profile | **Passed 2026-09-01:** cached paint 1.668 s and interactive 1.724 s; background roll analysis settled at 2.706 s; ordinary search including its 120 ms debounce took 152.3 ms |
| Cold startup and indexing | packaged app, isolated stale-cache 5,525-item profile | **Passed 2026-09-01:** cache miss reported separately; collection scan settled at 42.820 s, first interactive collection at 43.143 s, and roll analysis settled at 44.054 s |
| 20k Item Assistant migration | source-build app, synthetic 20,000-copy mixed-mode database plus queue receipt | **Passed 2026-09-01:** read-only preflight reported 20,001 source rows, SC/HC split, one unsupported record, exact backup/free-space requirements and destination mode; first run imported 20,000 copies in 995 ms through five bounded named stages; unchanged repeat completed in 663 ms with no duplicates, zero additional backup bytes, one verified backup reused, and source unchanged |
| 20k cached startup and archive renderer scale | packaged app, isolated cached 24,514-copy profile | **Passed 2026-09-01:** cached paint 2.544 s and interactive 2.597 s (automated 5 s budget); background roll analysis remained non-blocking; Transfers switches in 33.6 ms and mounts only bounded history or quarantine pages instead of 14,509 archived copies; search including debounce took 164.5 ms |
| Transfer operation histories | isolated desktop journal smoke plus cached 24,514-copy profile | **Passed 2026-09-01:** ingest and dispense operations are queried from the durable operation journal in read-only 50-row pages; a 20,090-result journal search took 155.4 ms including its 150 ms debounce and rendered exactly 50 operation rows; record, seed, SC/HC mode, outcome, timestamps, source, errors, and correlation IDs survive the IPC boundary; Ingest history, Dispense history, and Quarantined items are separate Back/Forward-restorable views, and only quarantine exposes return actions |
| Persistent system navigation | isolated Settings route at wide and narrow viewports plus Back/Forward automation | **Passed 2026-09-01:** Collection, Transfers, and Settings are visible native buttons that each receive focus; `aria-current=page` identifies Collection throughout its child workspaces; Collection returns directly from Settings without requiring history, while Back and Forward restore both destinations |
| Versioned typed application routes | `npm run test:routes`; isolated Collection/MI fixture at 1,440×1,000; isolated Sets fixture at 520×900; existing Settings/Collection navigation harness | **Passed 2026-09-01:** all 12 workspaces round-trip through bounded version-1 route decoding and URL hashes without snapshots, result arrays, observed-item data, or transfer payloads. A direct Collection deep link restored its query, item drawer, MI Workshop return path, and Back/Forward; native `pageshow` could not override MI controls. A direct Sets link restored the workspace and set-item drawer through Back/Forward at 520px with a 505px document and no horizontal overflow. The existing Settings → Collection → Sets history sequence also passed unchanged. |
| Product naming copy policy | deterministic renderer-visible source scan plus isolated onboarding, Settings, Transfers, and workspace-recovery captures at 1280×900 and 520×900 | **Passed 2026-09-01:** application actions and ownership use CC; all source lines containing Cairn match 27 exact formal product-name or 11 exact lore/content allowances; CSS-generated text is included; all eight representative layouts remain readable without clipping |
| Shared structured search | parser/builder unit gate, desktop SQL smoke, and cached 24,514-copy profile | **Passed 2026-09-01:** implicit/explicit Boolean operators, grouping, phrases, negation, aliases, Unicode text, booleans, and numeric comparisons share one parser; the schema-driven visual builder round-trips canonical queries and visibly preserves unsupported mixed groups; wide (1280×900) and narrow (520×900) isolated captures passed; invalid edits show an inline error without emptying the view; a `source:"item assistant" AND mode:hardcore AND seed:>=3100000000` history query matched 10,000 operations in 153.5 ms including its 150 ms debounce and rendered 50 rows |
| Shared semantic design foundation | `npm run test:theme-contract`, renderer typecheck/build, CSS parse, and isolated synthetic Settings/Sets captures | **Passed 2026-09-01 foundation slice:** 17 required semantic roles are present; all 9 shared Vue components are literal-palette-free; the legacy global stylesheet color count was reduced from 1,306 to a ratcheted 1,214; shared headers, explorer/advanced-search controls, badges, import feedback, system navigation, workspace tabs, segmented controls, pagination, Settings cards/actions, and common empty states consume tokens; populated Sets remained readable at 1,440×1,000 and 520×900 with filters stacked and rarity/readiness/FX semantics preserved. Full per-workspace migration remains tracked by #45. |
| Versioned theme extension contract | `npm run test:theme-contract`, renderer typecheck/build | **Passed 2026-09-01:** manifest version 1 exposes 53 shared dark-palette roles with token-level Cairn fallback; unsupported versions, malformed metadata, and contrast failures fail closed to the complete Cairn theme. All 13 Vue components are literal-color-free. Eleven rarity/gameplay colors are protected from theme overrides, remain unique, and retain at least 4.5:1 card-surface contrast. Static CSS recovery defaults and runtime Cairn values are synchronized by the gate; legacy `styles.css` remains ratcheted at 1,214 literals. |
| Focused specialist workspace shell | Isolated cached 24,553-copy profile; Collection, Skill Explorer, Leveling Planner, and Stash Oracle at 1,440×1,000 and 520×1,000; automated focus, bounds, visibility-gating, and Back/Forward checks | **Passed 2026-09-01:** Collection alone retains the archive hero, completion trackers, collection basis, and full tool launcher. Specialist content begins in the first viewport behind a compact switcher; Collection, active tool, and customization controls receive keyboard focus; the active destination scrolls into the bounded rail; hiding the active tool returns to Collection and restoring it reopens the focused shell. Specialist pages produced no document-level overflow at 520px. The existing narrow topbar and dense ExplorerToolbar overflow remains separately owned by #63. |
| Responsive specialist tools | Isolated cached 24,553-copy profile; Settings, MI Workshop, Skill Explorer, and Leveling Planner at 520×1,000 plus MI Workshop at 1,440×1,000; automated document-overflow, overlay-bounds, focus-restoration, and local-scroller assertions | **Passed 2026-09-01:** Settings shrank from the 785px audit baseline to a 505px document with no horizontal overflow; MI Workshop, Skill Explorer, and Planner also stayed at 505px. Shared search/filter/sort controls stack without losing field meaning. Search tips and Advanced search remain inside the viewport and restore trigger focus after Escape/close. The 1,180–1,250px comparison tables remain deliberately wide inside labeled, focusable local scrollers with visible compact-width guidance and sticky identity columns; the wide MI layout remained unchanged at 1,440px. |
| Shared modal focus and reduced motion | `npm run test:accessibility`, renderer typecheck/build, and source-build Electron gates | **Passed 2026-09-02 foundation slice:** Advanced Search, first-run onboarding, and Planner setup share one focus controller for initial focus, forward/reverse Tab wrapping, escaped-focus containment, modal history blocking, explicit Escape policy, listener cleanup, and deferred connected-target restoration with a logical fallback. Native Advanced Search and custom Planner setup passed real containment, exact captured-listener removal, Escape restoration, detached-trigger fallback, and overlay bounds checks. Reduced motion now collapses tokenized and legacy hard-coded transitions/animations and disables smooth scrolling. The gate prevents the four remaining App-owned private dialogs from increasing; their migration plus the complete Collection/Transfers/Settings/Sets/Planner/search assistive-technology audit remain open under #16. |
| Shared bounded result contract | `npm run test:bounded-results`, renderer typecheck/build, synthetic 20k/50k arrays, isolated MI Workshop, Skill Explorer, and Leveling Planner fixtures | **Passed 2026-09-01:** local 20,000- and 50,000-record collections mounted exactly one 50-record page; an over-delivered server page was capped at 50; page clamping, stable-key rejection, linear/responsive-grid keyboard movement, and none/single/multi keyed selection passed. Transfers history and quarantine use the shared loading/empty/range/paging surface; quarantine additionally preserves disabled selection while a recovery is busy. MI Workshop reported 72 affix combinations and Skill Explorer reported 120 Wendigo Totem items while each mounted exactly 50 rows at 1,440×1,000 and 520×900. Leveling Planner reported 120 supporting tiers while mounting 50 in both table and card modes at both widths; its empty result state passed, and favorite/ignore/restore actions remained independent of item activation. Focus opened the shared item tooltip, ArrowDown/End/Home moved focus, and Next/Previous replaced then restored each bounded page. The generated 20k archive profile remained a cache hit, became interactive in 3.581 s under the 5 s budget, searched in 150.8 ms including debounce, and mounted its 44 matching Planner tiers. |
| Shared bounded-grid accessibility ownership | `npm run test:bounded-grid-semantics`, accessibility contract, renderer/main typechecks and build, plus `npm run test:bounded-grid-semantics:electron` against the isolated `bounded-grid-a11y` fixture at 1,440×1,000 and 520×1,000 | **Passed 2026-09-02:** passive card grids retain `list` / `listitem`, while interactive, navigable, and selectable cards expose direct `grid` → `row` → `gridcell` ownership with one roving tab stop. Collection mounted 48/120 interactive cells; ArrowDown followed rendered columns after the semantic wrapper, ArrowUp returned to the first cell, and activation opened the item drawer. Stash Oracle mounted multiple navigable recommendations from two deterministic archived skill signals and passed the same bidirectional visual-row navigation. Supplies mounted 6/6 disabled selectable cells after its initial empty state; `aria-selected` and `aria-disabled` remained on each gridcell and Space could not select one. Wide and compact documents stayed contained at 1,440px and 505px with no document-level overflow. |
| Sets bounded result surface | `npm run test:sets-bounded-results`, renderer/main typechecks and build, plus `npm run test:sets-bounded-results:electron` against isolated detailed set fixtures at 1,440×1,000 and 520×1,000 | **Passed 2026-09-02:** the shared surface reported/mounted 3/3 normal cards, 0/0 empty cards, and exactly 50/202 cards for the generated scale fixture at both widths; its pure final-page projection retained sets 201–202. Stable set-record keys survived page replacement. Next received keyboard focus, sorting plus rarity/feature/search changes reset to the first page, and passive visual-grid cards exposed valid list/listitem semantics. An impossible search used the shared empty state; Back restored a page-two item route after that restrictive query without stale debounce clamping, and Forward restored the empty search route. Member focus/click retained the global tooltip and matching item drawer. Existing completion/readiness, recipe/awakening, rarity/effect, roll-summary, search, and sorting semantics passed unchanged. Wide and compact documents remained contained at 1,425 px and 505 px without page-level horizontal overflow. |
| Collection Farming bounded routes | `npm run test:farming`, exact 214-route source-build fixture, 1,440×1,000 and 520×900 captures, normal/empty searches, and automated paging/filter/tooltip interaction | **Passed 2026-09-01:** the ranked fixture reported 214 useful areas while mounting exactly 50 route cards at both widths; the final model page retained global ranks 201–214. Next received keyboard focus and replaced page one with ranks 51–100; changing rarity reset the view to rank 1. From page two, a one-route search reset to rank 1 with the expected stable route key, a zero-route search used the shared empty state, and clearing search restored the original first route identity. The normal route retained 12 item snippets plus its `+1 more` summary; hover opened the global tooltip and clicking a snippet opened the matching item drawer. Both widths had zero document-level horizontal overflow. |
| Collection Farming workspace extraction | `npm run test:farming`, renderer typecheck/build, generated `farming-routes` Electron fixture | **Passed 2026-09-01:** the route component owns query/rarity edits, page reset, toolbar, empty state, and bounded result markup; its view-model owns missing-item exclusion, location grouping, record deduplication, rarity/query filtering, minimum-level projection, and deterministic ranking. `App.vue` supplies catalog input, the typed Back/Forward control snapshot, and global search-document/icon/tooltip/item-drawer adapters. Complete route restoration retains its page while user query/rarity edits reset to page one. A direct compact-width `farming` deep link restored `query=route 003` plus `rarity=legendary` and mounted the exact 1/1 route. The existing 214-route paging, tooltip, filter-reset, 1,440×1,000, 520×900, and zero-result gates passed unchanged; compact document width remained 505px with 50 mounted routes. |
| Stash Oracle workspace extraction | `npm run test:oracle-workspace`, renderer typecheck/build, `--verify-oracle-workspace` against the isolated cached 24,553-copy profile at 1,440×1,000 and 520×900 | **Passed 2026-09-01:** the route component owns candidate generation, search, class/style/readiness/level edits, sort direction, page reset, and its bounded 12-card result surface; its view-model owns deterministic class projection, readiness counts, structured-query matching, and sorting. `App.vue` retains one typed Back/Forward control snapshot plus preference, planner-navigation, global tooltip, and item-drawer adapters. The full-range profile exposed 3,148 archetypes; `wendigo` matched 140 while mounting exactly 12. Keyboard traversal, next-page replacement, evidence hover through the global tooltip, impossible-query empty state, query-to-page-one reset, and 1–100 level controls all passed at 520px with a 505px document and no horizontal overflow. Complete typed-route restoration retains its requested page in the route contract. |
| Dismantling Lab workspace extraction | `npm run test:dismantling-workspace`, renderer typecheck/build, `--verify-dismantling-workspace` against the isolated cached 24,553-copy profile at 1,440×1,000 and 520×1,000 | **Passed 2026-09-01:** the route component/composable owns query/mode/rarity edits, progressive 120-copy disclosure, transient keyed selection, safe-duplicate selection, asynchronous preview state, and complete result/preview markup. Its pure model preserves catalogued ingested Epic/Legendary/MI/Rare eligibility, SC/HC and structured-query filtering, best-roll-then-newest copy retention, and attachment exclusion. `App.vue` retains one typed Back/Forward control snapshot, immutable vault input, a narrow read-only preview adapter, and shared redacted-error formatting; the workspace has no direct preload access and introduces no write path. The 24,509-candidate profile mounted 120 rows initially and exactly 240 after one keyboard-reachable disclosure, completed a real read-only one-copy preview, rendered its impossible-query empty state, reset to 120 after clearing search, preserved transient selection through Back/Forward, and stayed within 1,425px/505px documents without horizontal overflow. |
| Skill Explorer workspace extraction | `npm run test:skill-explorer-workspace`, renderer typecheck/build, `--verify-skill-explorer-workspace` against isolated synthetic data at 1,440×1,000 and 520×1,000 | **Passed 2026-09-03:** the route component owns the skill combobox, structured result query, availability/rarity/slot filters, level and modifier sorts, typed page state, and complete bounded table markup. Its pure model preserves direct-rank, special-modifier, and visual-only matching, skill/global damage-conversion labeling, lower-tier MI base collapse, structured search, and deterministic ordering. Level ascending is the initial and malformed-route fallback order. The fixture exposed 120 Wendigo Totem items while mounting exactly 50 rows; visual transformations rendered in their own column, item/level/rank cells retained the polished geometry, and the result count used balanced wide/compact layouts. Ordinary wheel input scrolled a deterministically overflowing tooltip and entering it canceled dismissal. Existing row/picker keyboard traversal, delayed pointer tooltip, paging, empty/reset states, and sortable-column semantics passed. Wide and compact documents stayed within 1,440px/505px without page overflow; the dense table remained in its labeled local horizontal scroller. |
| Supplies workspace extraction | `npm run test:supplies-workspace`, renderer typecheck/build, `--verify-supplies-workspace` against the isolated cached 24,553-copy profile at 1,440×1,000 and 520×1,000 | **Passed 2026-09-02:** the route component owns category, compatible-slot, query, transfer-mode, page, keyed selection, complete 60-card result markup, and transfer affordances. Its pure model preserves reusable-unlock deduplication, distinct potion instances, SC/HC supply identity, faction-reputation access, rune presentation, structured search, and slot filtering. `App.vue` retains one typed Back/Forward control snapshot, immutable catalog/archive input, the existing delayed global-tooltip adapter, and one dispense callback; the workspace has no direct preload access and changes no transfer safety checks. The 316-augment profile mounted exactly 60 cards before paging; keyboard focus and grid traversal, delayed global tooltip, page replacement, impossible-query empty state, query-to-page-one reset, live/offline mode restoration across Supplies → Transfers → Back/Forward → Supplies, and wide/compact document containment passed within 1,425px/505px documents without horizontal overflow. |
| MI Workshop workspace extraction | `npm run test:mi-workshop-workspace`, renderer typecheck/build, isolated 20,000-copy archive with `--verify-mi-workshop-workspace` at 1,440×1,000 and 520×1,000 | **Passed 2026-09-02:** the route component owns query, strict double-rare filtering, metric selection, sorting, direction, page, reserve disclosure, and complete 50-row table markup. Its pure model preserves exact affix rarity, grouped physical copies, strongest-copy leadership, stat and pet-stat metrics, structured affix/skill/damage search, and MI family identity. `App.vue` retains one typed Back/Forward control snapshot, the shared comparison drawer, and narrow catalog/icon/global-tooltip/item-drawer adapters. The focused gate rejects a magic/magic pair from double rares while retaining a rare/rare pair, selects the strongest grouped copy, searches affix presentation text, and sorts by required level; a generated 20,000-copy projection retained all copies in four groups in about 70 ms before the shared component's 50-row mount cap. The isolated archive exposed 1,774 combinations while mounting exactly 50. Immediate affixed keyboard tooltip, delayed pointer tooltip, paging, empty/reset state, ascending level sort, strict DOM rarity filtering, and labeled local scrolling passed at both widths; the wide and compact runs stayed within 1,425px/505px documents without page-level horizontal overflow. |
| Collection and materials workspace extraction | `npm run test:collection-materials-workspace`, renderer typecheck/build, isolated 120-item Collection fixture at 1,440×1,000 and 520×1,000, plus copied 24,514-copy profile for Components & Consumables at 520×1,000 | **Passed 2026-09-02:** one route component owns each workspace's independent typed controls, shared toolbar, category rail, sort behavior, and complete 48-card result surface; its pure model owns category/ownership/rarity/recipe/strict-double-rare/structured-query filtering and deterministic sorting. Raw query state updates typed history immediately while a cancelable 120 ms projection debounce coalesces rapid edits. The shell retains the Collection dashboard and narrow category-progress, search-document, icon, global-tooltip, item-drawer, and live-retrieval adapters. The 50,000-item focused projection stays below its 500 ms gate (roughly 20–35 ms on the test machine). Wide and compact Collection runs mounted exactly 48/120 cards and passed keyboard tooltip/grid navigation plus next/previous page replacement. The copied archive mounted exactly 48/167 material cards. Typed Electron navigation proved Collection and Materials retain different queries across repeated switching. All runs stayed within 1,425px/505px documents without page-level horizontal overflow. |
| Settings workspace extraction | `npm run test:settings-workspace`, renderer typecheck/build, isolated compact Settings deep links with `--verify-navigation` and `--verify-settings-workspace` in ordinary and safe mode | **Passed 2026-09-02:** the focused system workspace owns fourteen configuration cards, Item Assistant import entry point, visible/experimental tool form, archive scope and backup presentation, MI counting, retrieval target, supply behavior, game-data rebuild, quest-item recovery, diagnostics, preference export, and safe-mode controls. Its pure model owns stable tool definitions, onboarding status labels, and SC/HC archive-scope projection. `App.vue` retains persisted state plus narrow preload, confirmation, notification, refresh, and restart adapters; the workspace has no direct preload access. The executable component gate changes and verifies both v-models through persisted parent preferences, verifies emitted tool and experimental booleans, proves the final SC/HC archive mode cannot be disabled and both-mode state can be restored, and verifies safe mode disables auto-connect and experimental controls. The isolated 520×1,000 deep link also verifies Settings ↔ Collection ↔ Sets Back/Forward restoration, persistent system navigation focus/hit targets, and no document-level overflow. |
| Transfers workspace extraction | `npm run test:transfers-workspace`, renderer typecheck/build, isolated compact Transfers deep link with `--verify-transfers-workspace` | **Passed 2026-09-02:** the focused system workspace owns ingest/dispense/quarantine navigation, structured history and quarantine controls, remote bounded results, return-mode presentation, and quarantine selection. Its typed session is shared with route history and shell service adapters; historical operations remain read-only. `App.vue` retains all archive/recovery queries and mutations, live/offline delivery, safety refreshes, notifications, and preload access behind five callbacks. The executable 520×1,000 gate restores a dispense-history query, changes outcome, switches through quarantine and offline mode, proves empty quarantine keeps return disabled, restores history controls after remount, and verifies the resulting typed route state. The route matrix and visual capture stay within a 505px document without horizontal overflow. |
| Remaining bounded card tools | Isolated cached 24,553-copy profile; populated and filter-empty Collection, Components & Consumables, Stash Oracle, and Supplies at 1,440×1,000 and 520×900/1,000; page, keyboard, overflow, and supply eligibility assertions | **Passed 2026-09-01:** Collection mounted 48/5,525 cards, Components & Consumables 48/167, Stash Oracle 12/3,148 ranked archetypes, and Supplies 60/316 augments. Every compact run remained within a 505–520px document with no horizontal overflow. Collection focus opened the global item tooltip; ArrowDown/End/Home and Next/Previous replaced and restored its keyed page. Oracle's full-range Surprise me path preserved the ranked result order. With no active character, all 60 mounted augments remained explicitly disabled and synthetic card activation selected none. All four impossible searches rendered the shared zero-result state with zero mounted records. |
| Cross-workspace visual acceptance audit | source-build app, isolated generated 24,553-copy profile, 24 wide/narrow captures, 11 filter-empty runs, and DOM overflow/count instrumentation | **Baseline recorded 2026-09-01 under #58:** all 12 requested destinations were asserted before capture; every searchable workspace rendered a zero-result state; Collection mounted 48/5,525 cards, Skill Explorer 50/61 rows, Planner 44/44 rows, MI Workshop 50/1,774 rows, Oracle 12/3,148 cards, Dismantling 120/24,509 candidates, and Transfers 50/24,609 operations. Sets mounted all 202 cards and Farming all 214 route cards, originally assigned to #64/#65; Farming and the remaining Collection/Components/Oracle/Supplies card caps are now resolved by their dedicated shared-surface gates. The focused shell and narrow overflow findings were resolved by #62/#63. Full keyboard/assistive-technology acceptance remains #16. |
| Sets rarity and readiness semantics | unit gate plus isolated synthetic wide/narrow Electron fixtures | **Passed 2026-09-01:** Epic and Legendary cards use shared rarity tokens; archive recipe projection and awakening qualifications do not inflate discovered completion; storage, recipe, awakening, missing, level, completion, and FX badges remain explicit at 1,440 px and 760 px and meet the 4.5:1 text-contrast gate; `craftable:true OR awakening:true` matched the single qualified set in 154.2 ms including its 150 ms debounce |
| Leveling Planner creation and navigation | setup/migration and item-match unit gates plus isolated packaged 1,440 px and 520 px Electron fixtures | **Passed 2026-09-01:** Blank, character-save, and clone starting points share a four-step focus-contained dialog; combined masteries, complete indexed skill suggestions, mastery-wide item bonuses, and level windows persist without changing legacy plans; mastery matching is case-insensitive and deduplicated; the compact toolbar remains aligned at both widths; Back/Forward restores the active plan after creation |
| 20k archive roll hydration | packaged app, isolated 20,000 newly unscored copies across SC/HC | **Passed 2026-09-01:** all 24,509 applicable copies ended on roll model v4 with zero missing scores in 81.8 s; batches persisted 256 scores at a time and transmitted the full snapshot only at completion |
| Interrupted archive roll hydration | packaged app terminated during isolated 20k run | **Passed 2026-09-01:** 1,280 newly completed scores remained committed and the next run had exactly 8,720 active-mode copies left to process |
| Main-process service boundaries | `npm run test:ipc-services` and `npm run test:domain-services` with fake IPC, storage, helper, diagnostics, app, and window adapters | **Passed 2026-09-01:** 51 request channels have unique ownership across 8 concrete services; invalid inputs make zero service calls; valid inputs delegate once; ordinary known and unexpected failures cross Electron as redacted, versioned client errors; rejected writes do not poison the shared queue; import cancellation is pre-mutation and the exact archive commit boundary schedules protection; collection discovery/settings and 256-item hydration are service-owned; archive mutations serialize; transfer timeout, fresh-service restart, replay, rejection, mixed receipts, and uncertain outcomes resolve or fail closed; repeated quit events run one flush workflow; successful preload contracts remain unchanged |
| Inventor dismantling probability model | `npm run test:helper`, installed-data read-only sample | **Passed 2026-09-01:** the effective gdx3 rule, Master/Level/Dynamic graph, level limits, bell-slope weighting, blank no-drop weight, cost/scrap arithmetic, and unsupported-equation fail-closed behavior are covered; an installed five-item sample retained total bonus probability mass 4.8 across 48 eligible rewards. Exact RNG outcomes, Dynamite debit, generated-stack delivery, and reagent-store mutation remain deliberately blocked and documented. |
| Package personal-data/art audit | `scripts/audit-package.mjs` | Passed, 282 files |
| Installer install/first run/uninstall | `npm run test:installer` | Passed; app removed and isolated user data retained |
| v0.1.0-beta.2 combined release candidate | clean `d6fd0b8` baseline plus version-only release commit; `npm run test:release-local`, isolated packaged UI matrix, and independent hash audit | **Passed 2026-09-01:** repository verification passed twice; the read-only installed-game smoke indexed 1 installation, 2 save locations, 6 transfer stashes, and 5,525 catalog entries; both the 282-file portable package and 277-file installer payload passed provenance audits; install/first-run/uninstall retained isolated user data; ZIP and installer hashes matched the clean manifest; 0 dependency vulnerabilities were reported |
| v0.1.0-beta.2 packaged interaction and scale pass | packaged app, isolated fixtures, and copied 24,553-copy profile at 1,440×1,000 and 520×900 | **Passed 2026-09-01:** cached paint 2.471 s and interactive 2.528 s (5 s budget); a 335-result Collection search took 248.2 ms including debounce and mounted 48 cards; a double-rare Bloodsworn MI search returned one combination in 151.1 ms; onboarding, Settings, Transfers, Sets, Skill Explorer, and Leveling Planner were visually reviewed at representative widths; Back/Forward restored Settings, Dispense history, Quarantined items, and planner selection; the MI tooltip displayed exact prefix and suffix sections, mouse-wheel scrolling stayed inside the scrollbar-free tooltip, and Escape closed onboarding with focus restored to its launch button |
| Dependency vulnerability audit | `npm audit --audit-level=high` | Passed, 0 vulnerabilities |
| Tracked-file privacy/provenance audit | `npm run audit:repo` | Passed; native fingerprints verified |

The 2026-08-30 installed-game run indexed one installation, two save locations,
six transfer stashes, and 5,525 catalog items. It analyzed 97 owned copies and
withheld no roll scores as untrusted. The same run completed the isolated archive
backup/restore round trip without touching the user archive or game files.

The UI benchmark enforces the warm-start budget with
`node scripts/benchmark-ui.mjs --warm-budget-ms 5000`. Cold or stale-cache indexing is
measured as a separate scan phase and is not presented as cached startup latency.
Automated Electron gates opt into a Windows-only test fallback: when the first sandboxed
renderer exits specifically with `reason=launch-failed`, the harness records the exit details
and retries the disposable fixture once with Chromium's sandbox disabled. Crashes, OOM exits,
ordinary exits, signal termination, and timeouts remain failures; signaled parents also trigger
explicit descendant cleanup, and production windows stay sandboxed. On the
2026-09-02 local Codex host, exit code 49 was detected without the former two-minute wait and the
1,440×1,000 and 520×1,000 bounded-grid gates passed on the explicit fallback run.

| Skill Explorer perceivable selection and sorting | `npm run test:skill-explorer-workspace`, renderer typecheck/build, and `npm run test:skill-explorer-workspace:electron` at 1,440×1,000 and 520×1,000 | **Passed 2026-09-02:** Arrow Down/Up keep DOM focus on the editable Skill input while exposing one stable, selected `aria-activedescendant`; filtering preserves each skill's option ID, repeated traversal scrolls the active option into view, and listbox options do not add Tab stops. Pointer and Enter selection close the popup, while Escape closes without changing the selected value or retaining dangling ARIA references. The active sortable column alone exposes `aria-sort`, and its visible direction arrow is hidden from assistive technology. The dedicated Electron gate is part of `npm run verify`. |
| Global tooltip direct scrolling | `npm run test:bounded-results`, `npm run test:skill-explorer-workspace`, and `npm run test:skill-explorer-workspace:electron` at 1,440×1,000 and 520×1,000 | **Passed 2026-09-03:** the pointer-accessible, focusable tooltip retains a visible thin scrollbar and contained overscroll. Ordinary wheel input targeted at deterministic overflow advances its scroll position and prevents the underlying workspace from moving; wheel input elsewhere remains ordinary page input. Entering or focusing the tooltip cancels pending dismissal, and leaving schedules the established delayed close. |

## Manual live-transfer gates

These require Grim Dawn, deliberate sacrificial items, and human confirmation.
They are never inferred from a successful injection handshake.

| Scenario | Required observation | Result for release candidate |
| --- | --- | --- |
| Connect/disconnect | Exact fingerprint, ready handshake, clean disconnect, game survives | Pending |
| Epic/Legendary ingest | One exact item disappears, receipt commits, archive has seed/affixes | Pending |
| Same-copy retrieval | Exact copy returns and remains usable after game save/restart | Pending |
| MI with prefix/suffix and pet stats | Exact affixes, rolls, and pet stats survive round trip | Pending |
| Soulbound augment delivery | Exactly selected augment reaches active character | Pending |
| Multi-select delivery | Only selected records, one each, serialized acknowledgements | Pending |
| Full shared destination | Batch stops; unacknowledged archive copies remain stored | Automated rejected-retrieval rollback passed; live confirmation pending |
| Full character inventory | Personal delivery is rejected/returned without archive release | Automated generated-delivery rejection and partial-batch recovery passed; live confirmation pending |
| Repeated submit | UI/orchestrator produces no duplicate operation or duplicate item | Automated pending-copy and committed-ingest duplicate guards passed; live confirmation pending |
| Game exits mid-operation | Outcome remains pending/recoverable, queue payload retained | Passed live 2026-08-30 for five-item ingest; offline deposited/rejected retrieval and supply reconciliation passed 2026-09-01 |
| Cairn exits/restarts mid-operation | Journal and receipt reconcile without duplication | Automated on-disk close/reopen passed 2026-09-01; live timing confirmation pending |
| Stale or mismatched receipt | Semantic mismatch never commits or releases an archive copy | Automated fail-closed helper and desktop cases passed 2026-09-01 |
| Unsupported game rebuild | Live injection remains blocked; read-only tools still work | Automated fingerprint negative control passed; live confirmation on the next game update pending |
| Item Assistant migration | Verified backup; SC/HC multiplicity and queue receipts preserved; repeat creates no copies | Automated 20,000-copy core migration gate runs in `npm run verify`; WAL sidecars fail closed, the queue batch/manifest is hash-checked, an orphan batch is reconciled, and unchanged repeat creates no copies; user-path confirmation remains optional |

Record the exact app version, game version/build, `Game.dll`, hook, and injector
hashes with the completed manual run. A different hash starts a new matrix.

## Recovery expectations

| Operation | Terminal evidence after interruption | Expected archive state |
| --- | --- | --- |
| Live ingestion, one or many | Incoming queue file copied and hash-verified | Each queue identity commits once; a repeated poll only acknowledges the already-committed receipt |
| Archive retrieval | Every retained queue has a matching deposited receipt | Exact pending copies become retrieved once; reusable supplies remain stored |
| Archive retrieval | Every retained queue has a matching rejected receipt | Rejection is copied and acknowledged; every pending copy returns to stored |
| Generated/supply delivery | Deposited and rejected receipts are mixed | Operation commits with exact deposited and rejected counts; no archive copy is released |
| Any live delivery | Outgoing file still exists | Operation stays pending and later writes remain blocked |
| Any live delivery | No matching semantic receipt, including stale content | Operation stays `needs_recovery`; no optimistic commit or rollback occurs |

The automated 2026-09-01 run used Cairn Codex `0.1.0-beta.1`, helper protocol 1,
hook SHA-256 `419b53fdff4e75dafb98f9066a0271da0f0c937b5b02e5beca2e39af527a34c5`,
and injector SHA-256 `569e6bdde51148b29aece0491366e9aa4c21cf2f11279a94c815e2b958cfe10c`.
The clean combined `dcc3acea74b8e4476d750bd727f3c3103461a449` tester build produced app ASAR
SHA-256 `88cfa5bdd02a337c75299362b18bf55f117ab01f019d6f8d7d3ebcb6226820c4`,
self-contained helper SHA-256
`97c7efd67ddbfaf64d334ec6cf972eb204b1167c733cfe7bbab2bf1c3eb2aedc`,
portable ZIP SHA-256
`fada814ae50e6ac4c46b8740fde1aa6b9234b9fb4717286fe3020c86b45d0d30`,
and installer SHA-256
`dfe2c9cc99e44b8904f0d94fed8670e389fb77a644bb7a744ca89148668d709e`.
The live confirmation row must additionally record the exact supported `Game.dll` hash
reported by the support bundle; compatibility remains fail-closed when that fingerprint differs.

The `0.1.0-beta.2` release candidate retained helper protocol 1 and the same verified native
fingerprints: hook SHA-256
`419b53fdff4e75dafb98f9066a0271da0f0c937b5b02e5beca2e39af527a34c5` and injector SHA-256
`569e6bdde51148b29aece0491366e9aa4c21cf2f11279a94c815e2b958cfe10c`. Exact published
portable and installer hashes are carried by the release's `.sha256` and manifest assets.
